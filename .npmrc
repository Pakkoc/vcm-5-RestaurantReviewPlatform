# Ensure platform-specific native optional deps (e.g., lightningcss) are installed
optional=true

# Allow postinstall scripts that fetch/copy native bindings
ignore-scripts=false

# Make sure scripts resolve the right Node binary on Windows
scripts-prepend-node-path=true

# Keep installs clean and quiet
fund=false
audit=false

